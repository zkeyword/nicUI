{{#sprites}}

.iconSprite(){
	background-image: url({{{escaped_image}}});
}
.icon-{{name}}(@isHover: false){

	.hover (@a) when (@a = false) {
		.iconSprite();
		width: {{px.width}};
		height: {{px.height}};
	}
	.hover(@isHover);
	
	background-position: {{px.offset_x}} {{px.offset_y}};
}

[class^="icon-"],
[class*=" icon-"]{
	.iconSprite();
}
.icon-{{name}}{
	background-position: {{px.offset_x}} {{px.offset_y}};
	width: {{px.width}};
	height: {{px.height}};
}

{{/sprites}}